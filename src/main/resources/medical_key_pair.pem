# PLACEHOLDER — DO NOT COMMIT REAL PRIVATE KEYS
#
# Generate a real RSA key pair and supply it via an environment variable
# or a secrets manager (AWS Secrets Manager, HashiCorp Vault, etc.).
#
# Example (generate locally):
#   openssl genrsa -out medical_key_pair.pem 2048
#
# This file is intentionally empty/placeholder.
# The real key should be provided at runtime via the deployment environment.
