package com.medicalstore.controller;

/**
 * HomeController — routing placeholder.
 *
 * The root path "/" is handled by {@link ShopkeeperDashboardController}.
 * Owners are redirected to /owner in the authentication success handler
 * ({@link com.medicalstore.config.SecurityConfig}).
 *
 * This class is kept to avoid breaking any existing references.
 */
public class HomeController {
    private HomeController() { }
}
